# dev/test/pre addr
nacos.non.prod.addr: 127.0.0.1:8848

# local addr
nacos.local.addr: 127.0.0.1:8848

# prod addr
nacos.prod.addr: 127.0.0.1:8848


spring:
  cloud:
    nacos:
      config:
        extension-configs:
          - data-id: quick-global.yml
            group: ${spring.config.activate.on-profile}
          - data-id: ${spring.application.name}-application-${spring.config.activate.on-profile}.yml
            refresh: true
            group: ${spring.config.activate.on-profile}

---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    nacos:
      config:
        server-addr: ${nacos.local.addr}
        namespace: c145a648-0faa-4d35-be96-dda7502abce7

---
## 测试环境配置
spring:
  config:
    activate:
      on-profile: test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.non.prod.addr}
        namespace: b9c55a57-acfd-4b32-8924-a1110f3f7f62
---
## 预生产环境配置
spring:
  config:
    activate:
      on-profile: pre
  cloud:
    nacos:
      config:
        server-addr: ${nacos.non.prod.addr}
        namespace: e2614130-de27-4e27-8291-28d687595d3f
---
## 生产环境配置
spring:
  config:
    activate:
      on-profile: prod
  cloud:
    nacos:
      config:
        server-addr: ${nacos.prod.addr}
        namespace: c62d2628-8d14-46cf-ba44-3951fe8775f9

---